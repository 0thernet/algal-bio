[project]
name = "algal-bio"
version = "0.1.0"
description = "Portable, evidence-preserving biology research campaigns"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pytest==8.4.2"]

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [".", "src"]
addopts = "-ra"
